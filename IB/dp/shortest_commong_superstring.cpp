#include<bits/stdc++.h>
using namespace std;
// not solved.
int solve(vector<string> &strings) {
	int n = strings.size();
	for (int i = 0; i < (1 << n); i++) {
		int mask = i;
		for (int j = 0; j < n; j++) {

		}
	}
}

int main() {

}